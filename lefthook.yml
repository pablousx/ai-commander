commit-msg:
  commands:
    commitlint:
      run: npx --no-install commitlint --edit {1}

pre-commit:
  commands:
    dotnet-format:
      run: dotnet format --verify-no-changes
      fail_text: "Run `dotnet format` to fix formatting before committing."
