{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core capability for the local AI Commander settings window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
