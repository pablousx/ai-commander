{
  "name": "ai-commander-tooling",
  "private": true,
  "description": "Dev tooling for AI Commander (commit linting, git hooks)",
  "scripts": {
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "lefthook": "^1.6.0"
  }
}
